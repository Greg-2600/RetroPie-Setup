--- a/libretro-common/audio/resampler/drivers/sinc_resampler.c
+++ b/libretro-common/audio/resampler/drivers/sinc_resampler.c
@@ -13,6 +13,7 @@ static void *resampler_sinc_new(const struct resampler_config *config,
    double bandwidth_mod, enum resampler_quality quality, resampler_simd_mask_t mask)
 {
     (void)config;
+    (void)quality;
 
     resampler_sinc_t *re = (resampler_sinc_t*)calloc(1, sizeof(*re));
     if (!re)
--- a/libretro-common/audio/resampler/drivers/nearest_resampler.c
+++ b/libretro-common/audio/resampler/drivers/nearest_resampler.c
@@ -6,6 +6,7 @@ static void *resampler_nearest_init(const struct resampler_config *config,
    double bandwidth_mod, enum resampler_quality quality, resampler_simd_mask_t mask)
 {
     (void)config;
+    (void)quality;
 
     resampler_nearest_t *re = (resampler_nearest_t*)
         calloc(1, sizeof(*re));
--- a/libretro-common/audio/resampler/drivers/null_resampler.c
+++ b/libretro-common/audio/resampler/drivers/null_resampler.c
@@ -1,6 +1,7 @@ static void *resampler_null_init(const struct resampler_config *config,
    double bandwidth_mod, enum resampler_quality quality,
    resampler_simd_mask_t mask)
 {
+    (void)quality;
     return (void*)0;
 }



